{
  "dependencies": {
    "chai-as-promised": "^7.1.1",
    "mocha": "^10.2.0"
  },
  "name": "project-mocha",
  "version": "1.0.0",
  "scripts": {
    "test": "mocha",
    "dev": "nodemon --exec \"npm run test\" --watch" 
  },
  "devDependencies": {
    "chai": "^4.3.7",
    "nodemon": "^2.0.22"
  }
}
